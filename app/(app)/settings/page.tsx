import { Check, Database, GitBranch as Github, LockKeyhole, Server } from "lucide-react";
import { env, isGitHubAuthConfigured } from "@/lib/env";

export const metadata = { title: "Settings" };

export default function SettingsPage() {
  return <div className="page-stack"><div className="page-heading"><div><span className="eyebrow">Local configuration</span><h1>Settings</h1><p>Review provider configuration without exposing sensitive values.</p></div></div><div className="settings-grid"><section className="panel settings-card"><span className="settings-icon"><Github /></span><div><h2>GitHub connection</h2><p>OAuth credentials are read on the server and access tokens are stored in SQLite.</p></div><span className={`connection-badge ${isGitHubAuthConfigured ? "connected" : "missing"}`}>{isGitHubAuthConfigured ? <Check size={14} /> : null}{isGitHubAuthConfigured ? "Configured" : "Needs setup"}</span><dl><div><dt>Client ID</dt><dd>{env.GITHUB_CLIENT_ID ? "••••" + env.GITHUB_CLIENT_ID.slice(-4) : "Not set"}</dd></div><div><dt>OAuth scopes</dt><dd><code>read:user user:email repo</code></dd></div></dl></section><section className="panel settings-card"><span className="settings-icon"><Server /></span><div><h2>AI provider</h2><p>Ollama runs locally and receives only the bounded context needed for a request.</p></div><span className="connection-badge connected"><Check size={14} /> Local provider</span><dl><div><dt>Base URL</dt><dd><code>{env.OLLAMA_BASE_URL}</code></dd></div><div><dt>Model</dt><dd><code>{env.OLLAMA_MODEL}</code></dd></div></dl></section><section className="panel settings-card"><span className="settings-icon"><Database /></span><div><h2>Local database</h2><p>Prisma uses SQLite in development and keeps only auth, chat, analysis, and preferences.</p></div><dl><div><dt>Provider</dt><dd>SQLite</dd></div><div><dt>Stored GitHub repository data</dt><dd>None</dd></div></dl></section><section className="panel settings-card"><span className="settings-icon"><LockKeyhole /></span><div><h2>Safety defaults</h2><p>Repository code is never executed. AI-generated reviews and drafts are never posted automatically.</p></div><ul className="check-list"><li><Check /> Server-only secrets</li><li><Check /> Input validation</li><li><Check /> No automatic GitHub writes</li></ul></section></div></div>;
}
